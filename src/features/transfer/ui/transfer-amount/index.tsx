import type { BaseProps } from '@/shared/types'

interface TransferAmountProps extends BaseProps {
  dummy?: any
}

export default function TransferAmount({ dummy }: TransferAmountProps) {
  return <div>Transfer Amount Component </div>
}
