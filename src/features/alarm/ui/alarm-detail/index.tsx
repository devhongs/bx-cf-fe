import { type BaseProps } from '@/shared/types'

interface AlarmDetailProps extends BaseProps {
  dummy?: any
}

export default function AlarmDetail({ dummy }: AlarmDetailProps) {
  return <div>alarm detail : feature</div>
}
